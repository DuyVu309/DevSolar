<?xml version="1.0" encoding="utf-8"?>
<layout >
    <data>
        <variable
            name="dialog"
            type="com.example.duyvd1.utils.dialog.DialogYesNo" />
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/opacity_black_25">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/clMain"
            android:layout_width="270dp"
            android:layout_height="wrap_content"
            android:background="@drawable/bg_dialog"
            android:orientation="vertical"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintLeft_toLeftOf="parent"
            app:layout_constraintRight_toRightOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <TextView
                android:id="@+id/tvTitleYesNo"
                android:layout_height="wrap_content"
                android:layout_width="wrap_content"
                android:gravity="center_horizontal"
                android:maxLines="10"
                android:padding="@dimen/common_20dp"
                android:text="@{dialog.mTitle}"
                app:layout_constraintBottom_toTopOf="@+id/btnNo"
                app:layout_constraintLeft_toLeftOf="parent"
                app:layout_constraintRight_toRightOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <Button
                android:id="@+id/btnNo"
                android:layout_width="0dp"
                android:onClick="@{dialog::onNegative}"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_button_dialog_left"
                android:text="@{dialog.mLeftTitle}"
                android:textColor="@color/text_blue_press"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintEnd_toStartOf="@+id/btnYes"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/tvTitleYesNo" />

            <Button
                android:id="@+id/btnYes"
                android:layout_width="0dp"
                android:onClick="@{dialog::onPositive}"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_button_dialog_right"
                android:text="@{dialog.mRightTitle}"
                android:textColor="@color/text_blue_press"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toEndOf="@+id/btnNo"
                app:layout_constraintTop_toTopOf="@+id/btnNo" />

        </androidx.constraintlayout.widget.ConstraintLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>
